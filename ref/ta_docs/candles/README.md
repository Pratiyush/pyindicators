# candles/ — Candlestick Pattern Recognition

Detailed file: `candlestick_patterns` (catalog of all 61 TA-Lib CDL functions + scoring contract + the penetration parameter).

Treat as a sub-project: one class per pattern, shared body/shadow geometry helper, uniform ±100/0 (or ±1/0) output.
