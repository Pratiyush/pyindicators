"""User-defined indicators.

Drop a module here that declares ``@INDICATORS.register(...)`` classes and the plugin
autoloader will discover it — no core edits required. Kept as a package so the autoloader
walks it.
"""
