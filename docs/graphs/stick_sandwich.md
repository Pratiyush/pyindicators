# `stick_sandwich` — visualization ideas

> **StickSandwich, CDLSTICKSANDWICH** · category **candles** · reads `open, high, low, close` · outputs `stick_sandwich`

**What it measures.** a white candle trapped between two black candles whose closes coincide — a potential

## Where to draw it

**Markers on the price chart** — place a symbol at each bar the pattern fires (no line). Use an up-triangle below the bar for a bullish print and a down-triangle above for a bearish one.

## Scale & reference lines

No vertical scale — the markers sit on the price candles at the firing bars.

## Rendering each output

- `stick_sandwich` → the **main line**.

## Visual signals to highlight

- **Cluster with context** — only highlight prints that occur at support/resistance or with a trend filter.
- **Colour by direction** — green for bullish patterns, red for bearish.
- **Tooltip** the pattern name + the bar's OHLC on hover.

## Sketch

```
markers on price:

    price ╱╲   ▼      ← bearish print
         ╱  ╲ ╱╲
        ╱    ╲  ▲    ← bullish print
```

## Pairs well with

support/resistance levels and a trend filter to qualify the signal.

---
_Auto-generated by `scripts/build_graph_ideas.py` from the registry — ideas only, no plotting code._
