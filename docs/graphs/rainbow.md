# `rainbow` — visualization ideas

> **Rainbow Charts, Rainbow Moving Average** · category **trend** · reads `close` · outputs `rainbow_1`, `rainbow_2`, `rainbow_3`, `rainbow_4`, `rainbow_5`, `rainbow_6`, `rainbow_7`, `rainbow_8`, `rainbow_9`, `rainbow_10`

**What it measures.** a fan of ``num_ribbons`` SMAs where each is the SMA of the previous one — the

## Where to draw it

**Overlay on the price chart** — same axes as candlesticks, sharing the time x-axis. Plot it as a line tracking price; it is directly comparable to the close.

## Scale & reference lines

Same **price scale** as the candles (no separate axis). 

## Rendering each output

- `rainbow_1` → a line.
- `rainbow_2` → a line.
- `rainbow_3` → a line.
- `rainbow_4` → a line.
- `rainbow_5` → a line.
- `rainbow_6` → a line.
- `rainbow_7` → a line.
- `rainbow_8` → a line.
- `rainbow_9` → a line.
- `rainbow_10` → a line.

## Visual signals to highlight

- **Price/line crossovers** — close crossing above the line = bullish, below = bearish (shade or mark the cross).
- **Slope** — colour the line green when rising, red when falling.
- **Distance** — how far price has stretched from the line (mean-reversion hint).

## Sketch

```
price + line overlay:

    price ╱╲    ╱╲
         ╱  ╲  ╱  ╲____   ← close (candles)
    ────╱────╲╱────────   ← indicator line
```

## Pairs well with

volume, plus a momentum oscillator (RSI or MACD) in a second pane.

---
_Auto-generated by `scripts/build_graph_ideas.py` from the registry — ideas only, no plotting code._
