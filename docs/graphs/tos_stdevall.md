# `tos_stdevall` — visualization ideas

> **TOS Standard Deviation All, ThinkOrSwim StDevAll, TOS_STDEVALL** · category **statistics** · reads `close` · outputs `tos_stdevall_lr`, `tos_stdevall_l_1`, `tos_stdevall_u_1`, `tos_stdevall_l_2`, `tos_stdevall_u_2`, `tos_stdevall_l_3`, `tos_stdevall_u_3`

**What it measures.** a single least-squares regression line fit over the *entire* series, framed by

> ⚠️ **not causal** — forward-shifted; do not use the leading edge for live decisions

## Where to draw it

**A separate pane below the price chart**, aligned on the same time axis. Centre it on its zero/neutral line so positive vs negative is obvious at a glance.

## Scale & reference lines

Auto-scale to the data; add a **zero line** if it oscillates around zero.

## Rendering each output

- `tos_stdevall_lr` → a line.
- `tos_stdevall_l_1` → a line.
- `tos_stdevall_u_1` → a line.
- `tos_stdevall_l_2` → a line.
- `tos_stdevall_u_2` → a line.
- `tos_stdevall_l_3` → a line.
- `tos_stdevall_u_3` → a line.

## Visual signals to highlight

- **Zero-line crosses** — colour the area above zero green, below red; mark each cross.
- **Histogram** — for *_hist columns, draw vertical bars from the zero line (rising/falling colour).
- **Signal-line cross** — where a *_signal line is present, mark fast-crossing-signal events.
- **Divergence** vs price for momentum-type readings.

## Sketch

```
lower pane, centred on zero:

      + ┤    ╱╲
      0 ┤───╱──╲────  zero line
      − ┤  ╱    ╲╱
```

## Pairs well with

the raw price series it is computed from, for context.

---
_Auto-generated by `scripts/build_graph_ideas.py` from the registry — ideas only, no plotting code._
