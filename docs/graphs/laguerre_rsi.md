# `laguerre_rsi` — visualization ideas

> **Laguerre RSI, LRSI, Ehlers Laguerre RSI** · category **momentum** · reads `close` · outputs `laguerre_rsi`

**What it measures.** Ehlers' low-lag RSI computed on a 4-stage Laguerre filter rather than a moving

> path-dependent (its value depends on prior bars' state)

## Where to draw it

**A separate pane below the price chart**, aligned on the same time axis. It is a bounded oscillator, so it gets its own fixed vertical scale.

## Scale & reference lines

0–1 (a flag/probability). Treat ≥ 0.5 as 'on'.

## Rendering each output

- `laguerre_rsi` → the **main line**.

## Visual signals to highlight

- **Threshold breaches** — shade the region above/below the overbought/oversold guides.
- **Guide crossings** — mark when it crosses back through 70/30 (or 80/20).
- **Divergence** — connect price highs vs indicator highs; a disagreement is the headline signal.

## Sketch

```
lower pane, 0–100 with guides:

    100 ┤
     70 ┤╌╌╌╌╌╱╲╌╌╌╌╌  overbought
        │   ╱  ╲
     30 ┤╌╲╌╱╌╌╌╌╲╌╌╌  oversold
      0 ┤
```

## Pairs well with

the price chart above (for divergence) and a trend filter (SMA/ADX).

---
_Auto-generated by `scripts/build_graph_ideas.py` from the registry — ideas only, no plotting code._
