# `accbands` — visualization ideas

> **Acceleration Bands, ACCBANDS** · category **volatility** · reads `high, low, close` · outputs `accbands_lower`, `accbands_mid`, `accbands_upper`

**What it measures.** SMA-smoothed envelope whose width scales with the high-low range ratio.

## Where to draw it

**Overlay on the price chart as a channel/band** — draw the upper and lower lines and **shade the area between them**; plot the middle line dashed. Price riding the bands is the signal.

## Scale & reference lines

Same **price scale** as the candles (no separate axis). 

## Rendering each output

- `accbands_lower` → a **band edge**; shade between upper and lower.
- `accbands_mid` → a **band edge**; shade between upper and lower.
- `accbands_upper` → a **band edge**; shade between upper and lower.

## Visual signals to highlight

- **Band touches / breaks** — closes outside a band flag stretch or breakout.
- **Squeeze** — bands narrowing = low volatility, often before a move; shade the squeeze.
- **Walking the band** — successive closes hugging one band = strong trend.

## Sketch

```
price inside a shaded channel:

    ┌───────────────┐ upper
    │░░░╱╲░░░░╱╲░░░░░│  (shade between bands)
    │░╱░░╲░░╱░░╲░░░░░│ ← price
    └───────────────┘ lower
```

## Pairs well with

Bollinger %B or bandwidth, and a volume pane.

---
_Auto-generated by `scripts/build_graph_ideas.py` from the registry — ideas only, no plotting code._
